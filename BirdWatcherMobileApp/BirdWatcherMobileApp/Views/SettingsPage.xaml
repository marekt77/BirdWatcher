<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://xamarin.com/schemas/2014/forms"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             xmlns:d="http://xamarin.com/schemas/2014/forms/design"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             mc:Ignorable="d"
             x:Class="BirdWatcherMobileApp.Views.SettingsPage"
             Title="{Binding Title}">
    
    <ContentPage.Content>
        <StackLayout 
            Orientation="Vertical" 
            VerticalOptions="StartAndExpand">
            <Label Text="Server Details" 
                HorizontalOptions="Start" 
                FontSize="20" />
            <StackLayout 
                Orientation="Horizontal" Margin="15,5,0,0">
                <Label Text="Status: " FontSize="15"/>
                <Label Text="{Binding ConnStatus}" FontSize="15" />
            </StackLayout>
            <StackLayout Orientation="Horizontal" Margin="15,0">
                <Label Text="Server Address: " FontSize="15"/>
                <Label Text="{Binding ServerIP}" FontSize="15" />
            </StackLayout>
            <Button Text="Change Server" HorizontalOptions="CenterAndExpand" VerticalOptions="Start" />
        </StackLayout>
    </ContentPage.Content>
</ContentPage>