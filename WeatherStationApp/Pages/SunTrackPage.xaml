<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             xmlns:models="clr-namespace:WeatherStationApp.Models"
             x:Class="WeatherStationApp.Pages.SunTrackPage"
             Title="Sun Track">
    <VerticalStackLayout Margin="10,0,0,0">
        <CollectionView
            VerticalOptions="Fill"
            SelectionMode="Single" 
            ItemsSource="{Binding SunTrack}" >
            <CollectionView.ItemTemplate>
                <DataTemplate x:DataType="models:SunTrack">
                    <VerticalStackLayout Margin="0,10,0,0">
                        <Label Text="{Binding temperature}" FontSize="23" />
                        <Label Text="{Binding timestamp}" />
                    </VerticalStackLayout>
                </DataTemplate>
            </CollectionView.ItemTemplate>
        </CollectionView>
    </VerticalStackLayout>
</ContentPage>